package 코딩테스트연습.level2;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class 후보키 {
    public int solution(String[][] relation) {
        int answer = 0;

        List<Boolean> cKey = new ArrayList<>(relation[0].length + 1);
        cKey.set(0, true);
        int r = 1;
        while(cKey.contains(false)) {

        }

        return answer;
    }
}
